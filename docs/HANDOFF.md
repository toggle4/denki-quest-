# HANDOFF — 引き継ぎ書（Claude Code 向け）

この文書は、claude.ai のチャットで決めた設計・データ・方針をすべてまとめたもの。
Claude Code はまずこの文書を読み、「現在のタスク」から作業を始めること。
チャット側は今後も教材データ（lessons / units）を作り続け、Claude Code はアプリ本体を担当する。

---

## 1. プロジェクトの目的

未経験者が中学理科の復習から始めて第二種電気工事士（筆記・技能）に合格するための iOS 学習ゲーム。
- 目安学習時間 200 時間。ただし期間の記述はアプリに入れない
- **基礎固め**と**網羅性**を最重視。計算問題は数値を変えて繰り返せる（テンプレート問題）
- 集中が途切れない設計：1セッション 5〜8 分、説明と問題を交互に、コンボ、ボス戦
- 品質が良ければ App Store 公開を検討。過去問や他人の画像は使わない（オリジナル問題・自作SVG）

## 2. 経緯（何が起きたか）

1. 以前、別の Claude Code セッションで第二種電気工事士の学習ゲーム（以下「旧ゲーム」）を作っていた。ユーザーはそのUI・構成を気に入っている。
2. その後 claude.ai のチャットで教材（基礎ステージのテキストと問題データ）と設計書を作った。
3. チャット側は旧ゲームの存在を知らず、「ゼロから作る」指示を出した。Claude Code が新しく DenkiQuest プロジェクトを作り、旧ゲームが見えなくなった（上書きの可能性）。
4. **ユーザーの希望：旧ゲームを復元し、そのUIを土台にして、チャットで作った教材データと設計を継ぎ合わせる。新規に作り直さない。**

## 3. リポジトリの現在の中身

```
CLAUDE.md                 プロジェクトの憲法（毎回読む）
docs/curriculum.md        全ステージ・全単元の順序と設計思想
docs/HANDOFF.md           この文書
content/schema.md         問題データ（JSON）の仕様。template 形式を含む
content/lessons/README.md 教材テキスト（Markdown）の仕様と画面遷移ルール
content/lessons/F01.md〜F08.md  ステージ0の教材テキスト（各4〜7セッション）
content/units/F02.json    F02 の問題データ（試作。差し込み問題つき）
DenkiQuest.xcodeproj ほか チャット側の指示で新規作成された SwiftUI 実装（テキストビューのみ）
```

## 4. 確定している設計（変更禁止。変えるときはユーザーに確認）

### 4.1 カリキュラム
- docs/curriculum.md のとおり。ステージ0（基礎12単元）→1（図記号・器具）→2（配線図）→3（施工・検査・法令）→4（配電理論・配線設計）→5（技能）
- ステージ0はF01〜F08（中学理科範囲）が作成済み。F09〜F12（交流・磁気・インピーダンス・三相）はチャット側で今後作成

### 4.2 説明と問題を混ぜる（最重要の設計変更）
- 「テキストを全部読んでから問題」ではない。**説明画面の直後に、いま読んだ概念の問題が1〜2問差し込まれる**
- 教材 Markdown の `<!-- quiz: id, … -->` が差し込み位置、`<!-- session-quiz: … -->` がセッション末のまとめ問題
- 詳細は content/lessons/README.md「画面の流れ」「戻る・進むのルール」

### 4.3 戻る・進むのルール
- 説明画面の間は自由に前後できる
- 問題画面に入ったら、前の説明画面には戻れない（戻るボタンなし）
- 問題を終えて次の説明に進んだら、直前の問題画面より前には戻れない
- 途中で閉じたら「最後に終えた問題画面の直後」から再開
- 既読セッションの読み直しは単元マップ経由で「復習モード」（説明のみ、問題なし）

### 4.4 問題データ
- content/schema.md のとおり。type は multipleChoice / numericInput / trueFalse / matching / imageChoice / **template**
- template は variables・constraints・answerFormula・distractors で数値をランダム生成。間隔反復の履歴はテンプレート単位
- 不正解 → 解説 → 同じ問題（template は数値を変えて）再出題。2回連続不正解なら正解を示して先へ
- 単元クリア：直近10問の正答率 90%以上（ステージ0は95%）＋ボス戦（`boss.pick` 問を `timeLimitSeconds` 内に連続正解）
- 間違えた問題は 1日→3日→7日→14日 後に再出題（SwiftData で管理）
- 弱点検知：同じ template で difficulty 2 以上を3回連続で間違えたら difficulty 1 に戻す

### 4.5 技術
- Swift / SwiftUI、iOS 17 以上、SwiftData、外部ライブラリ原則なし
- content/ はフォルダ参照で Bundle に入れる。教材ファイルの中身はアプリ側で書き換えない
- 図は Assets/figures/<名前>.svg。未作成のものは図名を書いたプレースホルダ表示
- xcodebuild でのビルド確認は `-destination 'generic/platform=iOS Simulator'` を使い、プロジェクトの署名設定（CODE_SIGN_*）は変更しない（過去に署名エラーの原因になった）

### 4.6 作業ルール
- 1回の依頼で1機能
- 変更後は必ずビルド確認してコミット
- 出題形式・Markdown 記法を追加したら schema.md / lessons/README.md も更新
- **既存の動くコードを作り直さない。拡張する**

---

## 5. 現在のタスク（この順で実行）

> **進捗（2026-09-18）**
> - タスク A 完了。旧ゲーム = GitHub の `claude/trusting-dirac-tprx3b` ブランチ（失われていなかった）。ユーザー確認済み
> - タスク B: 旧ゲームのブランチをそのまま土台にしたため、手順 1（legacy/ への取り出し）は不要。手順 2 は docs/legacy-overview.md。手順 3〜5 を実施中
> - チャット側の新実装（Mac の ~/Documents/denki-quest）から LessonParser / LessonLibrary / LessonBlockView / Curriculum を移植し、テーマを旧ゲームに合わせた
> - 過去問 9 回分を「型」に分類（docs/exam-patterns.md）。型からオリジナルの試験型ドリル 175 問（うち template 43）を作成し、ホームの「試験型ドリル」に表示
> - 問題データに `origin`（英語の語源）と `tip`（現場の豆知識）を追加し、解説の下に表示。72 問に記入済み
> - ホームのゲージ下に「昨日の学び」カード（前日に読了したセッションの **まとめ** を表示）
> - 単線図 SVG 7 枚（tools/gen_wiring_figures.py）と、最少電線本数・スリーブ数の型 W03/W04（23 問）+ u15 に 6 問。問題画面が図を表示するようになった
> - ボス戦を実装（BossEngine / BossBattleView）。ユーザー提供のモンスター画像 Assets.xcassets/BossMonster。
>   正解で攻撃（3 秒以内クリティカル 2 倍、6 秒 1.5 倍、10 秒 1.2 倍、コンボで最大 +50 %）、不正解は反撃でコンボが途切れる、制限時間内に HP 0 で勝利。負けは時間切れと降参だけ（ハートは廃止）。
>   入口: ドリルの結果画面「ボス戦に挑む」、教材単元画面の「ボス戦」カード（boss.questionIds を使う）
> - 間隔反復を SwiftData で実装（ReviewItem / ReviewScheduler）。ドリル・教材の差し込み問題・ボス戦のどこで間違えても翌日に再出題。
>   復習で正解するたび 3→7→14 日と伸び、14 日後に正解で卒業。ホームの「復習」カードから期限の来た問題を最大 10 問。
>   ドリル開始時はその単元の復習待ちを先頭に最大 3 問混ぜる（curriculum.md「スパイラル復習」）
> - タスク C の 1〜3 が完了。残り: 単元アンロック、模擬試験モード、図解ライブラリ、図記号 SVG
> - 試験範囲の教材テキストを Claude Code 側で作成開始: K01〜K09、D01〜D06、S08、G03、W03（18 本、差し込み問題つき）。
>   検証は tools/validate_lessons.py。教材単元画面に「ドリル（10 問）」「ボス戦」カードを追加
> - ボスを 12 体に拡張（DenkiQuest/Models/Boss.swift の BossRoster）。ステージを前半・後半に分け、区間ごとに 1 体が担当する。
>   割り当て表は docs/curriculum.md「付録：ボスの割り当て（12 体）」。画像は Assets.xcassets/Bosses/BossNN.imageset/boss_NN.png（01〜12、背景透過 PNG）。
>   仮画像は tools/gen_boss_placeholders.py で生成しているので、同じファイル名で上書きすれば差し替わる
> - 戦闘中は名前だけ、二つ名は登場演出とボス図鑑だけ。未撃破のボスは影と「？？？」で伏せる
> - マスコットのショート演出を強化: 通常 Mascot とこげた MascotBurnt の 2 枚を用意し、
>   6 秒ショートで姿が入れ替わる。頭から煙が 3 筋、黒いすすが舞い落ち、ふらふら傾く。
>   一言は「こげた… ちょっと休ませて」。絵の差し替えは tools/install_mascot_burnt.sh。
>   こげた状態は onBurntChanged でホーム（UnitListView）に伝わり、見出しの文が変わって画面のふちがすすける
> - 不具合修正: ボス戦の上部でバー・タイマー・ハートが画面右へはみ出していた。
>   原因は scaledToFill したボス画像が提案より大きい寸法を返し、ZStack の兄弟だった
>   ステータス列がその幅（実測で 393pt に対し約 418pt）で組まれていたこと。
>   絵を background と Color.clear の overlay に移し、レイアウトへ影響しないようにした
> - ボス戦上部の並びを変更: 名前を中央に大きく、その下に BOSS バッジ、HP バー、
>   HP 数値・コンボ・タイマーの行、残り時間バー。ステージの高さは画面の 46 %
> - App Store への提出で「iPad の画面分割には 4 方向の向きが必要」と弾かれたため、iPhone 専用にした
>   （TARGETED_DEVICE_FAMILY = 1、縦向き固定はそのまま）。iPad にも入るが iPhone 表示で動く。iPad 用のスクリーンショットは不要
> - マスコットの長押しを作り直し: 回るゲージを廃止し、指の上へ浮き上がる（指で隠れない）。指先から稲妻でつながり、
>   横に電圧計（0〜6.60kV）を出す。指がずれても離すまで充電が続く（DragGesture）。75 % から画面全体が震え、
>   ふちが 6 秒かけて光る。ショートで停電してちらつき、点き直す。ショート寸前で離すと「ギリギリ記録」（UserDefaults: mascot.bestVoltage）
> - 教材 F01〜F08 の図 34 枚を作成（tools/gen_lesson_figures.py → Assets.xcassets/Figures/<名前>.imageset）。
>   数値は本文の例題とそろえた。文字も線で描き、日本語は入れていない。色は 電流=青・電圧=赤・電力/熱=橙・抵抗=紺。
>   確認用 PNG は `python3 tools/gen_lesson_figures.py --preview <フォルダ>`（cairosvg が必要。アプリには不要）
> - ボス戦のハートを廃止（ライフとして機能していなかったため）。不正解は反撃とコンボ切れのみ。
>   二つ名は登場中だけ、名前の上に別の行で黄色く出す。戦闘に入ると名前だけになる
> - ボス戦の UX を作り込み: 3・2・1・GO! のカウントダウン、ダメージ倍率ゲージ（10 秒で減る）、
>   解答演出のあいだはタイマー停止、外した問題はその場で解説、残り 10 秒・5 秒の警告、
>   HP 3 割以下で ENRAGED（呼吸と稲妻が激しくなる）、降参ボタン、
>   結果画面に S/A/B/C ランクと取りこぼした問題の見直し、視差を減らす設定への対応
> - ボス図鑑を追加（BossCollectionView）。撃破回数・最速タイム・最大コンボ・最大ヒット・初撃破日を UserDefaults に保存（BossCollection）。
>   入口はホームの「ボス図鑑」カードと歯車メニュー
> - つまずきやすい所の補強: F05 にセッション5（ブリッジを比で見る）、F07 にセッション6（長さと直径を倍率でまとめる）、
>   F08 にセッション6（電圧降下と損失の取り違え）・セッション7（抵抗の与え方・負荷が抵抗のとき・逆算）を追加。
>   既存セッションの本文は変えていない。問題は content/units/F05.json・F07.json・F08.json（template 16 問を含む 25 問、ボスつき）。
>   図 3 枚を追加（F05_bridge_ratio、F07_length_and_diameter、F08_per_km）
> - template に `derived`（途中の値）を追加（QuestionSetV2.swift の DerivedSpec、TemplateEngine.generate）。
>   解説に「電流 {I}A → 損失 {answer}W」のように途中の値を出せる。仕様は content/schema.md。
>   template の生成チェック `python3 tools/check_templates.py`

### タスク A：旧ゲームの復元調査（変更なし、報告のみ）
1. `git log --all --oneline` と `git reflog` で、現在より前に別の Swift ファイル・Xcode プロジェクトがあったコミットを探す
2. `~/.claude/projects/` 以下の全 .jsonl を日付順に一覧し、「電気工事士」「クイズ」「SwiftUI」「Xcode」を含むセッションを特定。そのセッションで作成・編集されたファイル名と、可能ならコード本体を抽出
3. `~/Documents` `~/Desktop` `~/Downloads` の *.xcodeproj を一覧（最終更新日つき）
4. 見つかった候補ごとに「場所・日付・主要画面・機能」を報告し、**ユーザーに確認を求める**（どれが旧ゲームか）

### タスク B：復元と統合（ユーザーが旧ゲームを確定したあと）
1. 旧ゲームのコードを `legacy/` に取り出す（git のコミットから、または .jsonl から書き戻す）
2. 旧ゲームの構成（画面・ナビゲーション・データモデル・演出）を docs/legacy-overview.md にまとめる
3. **旧ゲームのUI・構成を土台**にし、次を継ぎ足す
   - content/lessons/*.md のパーサ（現 DenkiQuest 実装から流用可）
   - content/units/*.json の読み込みと template 生成（schema.md）
   - 説明↔問題の差し込み画面（4.2〜4.3）
4. 現 DenkiQuest 実装のうち、旧ゲームに無いもの（Markdown パーサ等）だけを移植し、重複する画面は旧ゲーム側を採用
5. F01〜F08 が旧ゲームのステージ0として表示され、F02 で「読む→解く」が一周動く状態にする
6. ビルド確認、コミット

### タスク C：以降の機能追加（1つずつ）
1. SwiftData による進捗保存と再開位置
2. 間隔反復（1/3/7/14日）と日次セッション構成（新規60%・復習30%・基礎ドリル10%）
3. ボス戦・コンボ演出
4. 単元アンロック（curriculum.md の順序）
5. 模擬試験モード（ステージ2到達で解放）
6. 図解ライブラリ（全単元の図・器具を一覧）
7. Assets/figures の SVG（チャット側が作成、または Claude Code が描画）

### タスク D：旧ゲームが見つからなかった場合
- ユーザーに報告し、現 DenkiQuest 実装を土台に B-3 以降を進める許可を得る

---

## 6. チャット側が今後届けるもの
- F01, F03〜F08 への差し込み問題マーカーと、対応する content/units/F0x.json
- F09〜F12 の教材テキストと問題データ
- ステージ1以降の教材（図記号は SVG 付き）
- 図（Assets/figures/*.svg）

Claude Code はこれらが追加されたとき、コードを変えずに読み込めるようにしておくこと。

---

## 7. 最初に実行するプロンプト（ユーザーが貼る文）

```
docs/HANDOFF.md を読み、「5. 現在のタスク」のタスクAを実行してください。まだ何も変更せず、報告だけしてください。
```
